# Tuned for a low-RAM machine: one JVM, modest heap, no daemon.
org.gradle.daemon=false
org.gradle.jvmargs=-Xmx900m -XX:MaxMetaspaceSize=300m
org.gradle.workers.max=1
kotlin.compiler.execution.strategy=in-process
