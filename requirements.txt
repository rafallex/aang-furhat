# --- core (required to run aang_app.py) ---
websocket-client>=1.6      # Furhat Realtime API (WebSocket) client
requests>=2.31             # LLM brain (Groq / Anthropic HTTP)

# --- Avatar-State deep voice (aang/avatar_voice_fx.py) ---
# Without these the Avatar lines fall back to the robot's native deep voice.
edge-tts>=6.1,<7           # keyless neural TTS (the base voice); capped: edge-tts churns its API across majors
pydub>=0.25,<1             # WAV handling / normalize for the deep voice render (pitch comes from edge-tts, not pydub)
imageio-ffmpeg>=0.4        # bundles the ffmpeg binary pydub needs (no system install)

# --- Avatar-State wind SFX (aang/sfx.py) ---
numpy>=1.24                # wind whoosh synthesis

# --- building the custom face (face/build_aang_face.py) — build-time only ---
Pillow>=10.0               # bakes the arrow into the skin texture

# For a fully reproducible demo machine, freeze exact versions:  pip freeze > requirements.lock
