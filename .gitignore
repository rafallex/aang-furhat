# secrets / local env
.env
*.env
!.env.example

# Claude Code session + memory (private)
.claude/

# python
__pycache__/
*.pyc
*.pyo

# generated Avatar-State audio (throwaway; the shipped whoosh.wav stays tracked as the no-numpy fallback)
aang/_sfx/*
!aang/_sfx/whoosh.wav

# Built face pack — the zip is a derived build artifact (rebuild via build_aang_face.py).
# The source textures (face/Aang/) and preview ARE committed so the repo is self-contained.
face/Aang.zip

# editors / IDEs
.idea/
.vscode/

# JVM crash / replay logs
*.log
