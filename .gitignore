# secrets / local env
.env
*.env
!.env.example

# Claude Code session + memory (private)
.claude/

# python
__pycache__/
*.pyc
*.pyo

# robot-fetched audio dir: throwaway rendered deep-voice files (rage.mp3/_raw.wav/.wav)
# are ignored; the committed wind asset + its builder stay tracked.
sfx/*
!sfx/whoosh.wav
!sfx/build_whoosh.py

# Built face pack — the zip is a derived build artifact (rebuild via build_aang_face.py).
# The source textures (face/Aang/) and preview ARE committed so the repo is self-contained.
face/Aang.zip

# editors / IDEs
.idea/
.vscode/

# JVM crash / replay logs
*.log
